Place the AvantGarde font file(s) here.

Expected by app/globals.css (@font-face family "AvantGarde"):
  - AvantGarde.woff2   (preferred)
  - AvantGarde.woff    (optional fallback)

A single variable font covers all weights (100-900). If you instead have
separate weight files (e.g. Book / Medium / Demi / Bold), let me know and
I'll add one @font-face rule per weight.

Until a file is present here, text falls back to Century Gothic / Futura.
